document.addEventListener('load', () => {
    //carrega tudo (DOM, CSS, IMAGENS)
    //Porem, mais lento
})
document.addEventListener('DOMContentLoaded', () => {
    //espera carregar apenas DOM
    //mais rápido
})
